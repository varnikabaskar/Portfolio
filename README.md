# Varnika B — Portfolio

A single-page, high-tech animated portfolio built with plain HTML/CSS/JS (no build step, no dependencies besides Google Fonts).

## Run locally
Just open `index.html` in a browser — or serve it:
```
npx serve .
```

## Deploy
Static site — deployable as-is to Vercel, Netlify, GitHub Pages, or any static host.
